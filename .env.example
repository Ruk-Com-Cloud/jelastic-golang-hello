# Example environment variables
# Copy this file to .env and modify the values as needed

# ===================
# Legacy Format (Still Supported)
# ===================
# Server Configuration
PORT=3000
HOST=0.0.0.0

# Database Configuration (Legacy)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=testdb
DB_PORT=5432
DB_SSLMODE=disable

# App Configuration (Legacy)
TEST_MSG=Hello from Docker Environment
ENVIRONMENT=development

# ===================
# Alternative: Use DATABASE_URL instead of individual DB_ variables
# ===================
# DATABASE_URL=postgres://postgres:password@localhost:5432/testdb?sslmode=disable

# ===================
# New Structured Format (Recommended)
# ===================
# You can also use the structured format with nested configuration:
# SERVER_PORT=3000
# SERVER_HOST=0.0.0.0
# DATABASE_HOST=localhost
# DATABASE_USER=postgres
# DATABASE_PASSWORD=password
# DATABASE_DBNAME=testdb
# DATABASE_PORT=5432
# DATABASE_SSLMODE=disable
# APP_TEST_MESSAGE=Hello from Docker Environment
# APP_ENVIRONMENT=development